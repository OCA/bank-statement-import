- **Mercury Plus invoicing** — create Mercury invoices from Odoo sale orders and sync
  payment status back via the Mercury Invoicing API.
- **OAuth2 multi-tenant** — support OAuth2 flow for users who prefer not to manage API
  keys manually (e.g. Odoo.sh / SaaS deployments).
